# SYS_flat-index — the flat slug index, before domains

**Status** — ARCHIVED. Superseded by the split into `platform/` and `catalog/`.

Kept because the reasoning is still cited: a single flat index owned both storage and
ordering, so every change to the reader's ordering touched the persistence path. The split was
made at that joint, not at a size threshold.

Nothing here is in force. A fiche under `archive/` that does not say so in its opening line is
indistinguishable from current direction, which is why R4 exists.
